---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: medicine-assistant-data-pvc
  annotations:
    volumeType: local
    volume.kubernetes.io/selected-node: worker2
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
