---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teamspeak-6-pvc
  annotations:
    volumeType: local
    volume.kubernetes.io/selected-node: worker1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 30Gi
